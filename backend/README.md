# Memory Architect — Backend

Minimal Node.js/Express API server for Memory Architect. Currently a starting point for authentication and player-progress endpoints consumed by the Unity client (`../frontend`).

## Run

```bash
npm install
npm start
```

Server listens on `http://localhost:3000`.
